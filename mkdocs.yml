site_name: Etl join excel files
theme:
  name: material
  palette:
    primary: deep blue
  language: pt

repo_name: ""
repo_url: ""

markdown_extensions:
  - pymdownx.superfences:
      custom_fences:
        - name: mermaid
          class: mermaid
          format: !!python/name:pymdownx.superfences.fence_code_format

plugins:
  - search
  - mkdocstrings:
      default_handler: python
